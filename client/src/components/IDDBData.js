import React from 'react';

const IDDBData = ({username }) => {
  return (
    <div className="IDDBData">
      <div>------------------------------</div>
      <div>아이디: {username}</div>
    </div>
  );
};

export default IDDBData;